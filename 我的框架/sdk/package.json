{
  "name": "sdk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:source": "cross-env MIN=false node build.js",
    "build:min": "cross-env MIN=true node build.js",
    "build": "cross-env MIN=all node build.js"
  },
  "author": "meiminjun",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^5.1.3",
    "rimraf": "^2.6.2",
    "webpack": "^3.10.0"
  }
}
